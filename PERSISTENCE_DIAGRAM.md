# Persistence Architecture - Visual Diagrams

## 🏗️ System Architecture

```
┌─────────────────────────────────────────────────────────────┐
│                     Browser Window                          │
│                                                             │
│  ┌───────────────────────────────────────────────────────┐ │
│  │          Workspace Context (Global State)             │ │
│  │  ┌─────────────────────────────────────────────────┐ │ │
│  │  │  verificationReports: Map<path, report>          │ │ │
│  │  │  notebookDiscrepancies: Map<path, issues[]>      │ │ │
│  │  └─────────────────────────────────────────────────┘ │ │
│  │                      ⬆️  ⬇️                            │ │
│  │              setVerificationReport()                  │ │
│  │              getVerificationReport()                  │ │
│  │              setNotebookDiscrepancies()               │ │
│  │              getNotebookDiscrepancies()               │ │
│  └───────────────────────────────────────────────────────┘ │
│                           ⬆️  ⬇️                            │
│  ┌─────────────────┐  ┌─────────────────┐  ┌──────────┐  │
│  │ ModelCard       │  │ NotebookViewer  │  │ Other    │  │
│  │ Viewer          │  │                 │  │ Components│ │
│  │ ├─ Content Tab  │  │ ├─ Code Cells  │  │          │  │
│  │ └─ Verify Tab   │  │ └─ Highlights  │  │          │  │
│  └─────────────────┘  └─────────────────┘  └──────────┘  │
│                                                             │
└─────────────────────────────────────────────────────────────┘
                           ⬆️  ⬇️
                    ┌──────────────────┐
                    │   localStorage   │
                    │  ┌─────────────┐ │
                    │  │ verification│ │
                    │  │   Reports   │ │
                    │  ├─────────────┤ │
                    │  │  notebook   │ │
                    │  │discrepancies│ │
                    │  └─────────────┘ │
                    └──────────────────┘
```

## 🔄 Data Flow: Saving Verification

```
User Action
    │
    ▼
┌─────────────────────┐
│ Click "Verify"      │
│ Button              │
└─────────────────────┘
    │
    ▼
┌─────────────────────┐
│ API Call to         │
│ CodeAct Agent       │
│ (10-30 seconds)     │
└─────────────────────┘
    │
    ▼
┌─────────────────────┐
│ Receive Response    │
│ { report: {...} }   │
└─────────────────────┘
    │
    ▼
┌─────────────────────────────────────┐
│ saveVerificationReport(path, report)│
└─────────────────────────────────────┘
    │
    ├──────────────────┬─────────────────┐
    ▼                  ▼                 ▼
┌─────────┐      ┌─────────┐      ┌──────────┐
│ Update  │      │ Save to │      │ Trigger  │
│ Context │ ───► │localStorage│ ───► │ Re-render│
│ Map     │      │ (async)  │      │          │
└─────────┘      └─────────┘      └──────────┘
    │
    ▼
┌─────────────────────┐
│ UI Updates          │
│ ✓ Badge shows score │
│ ✓ Tab enabled       │
│ ✓ Results visible   │
└─────────────────────┘
```

## 📖 Data Flow: Loading Verification

```
Component Mounts
    │
    ▼
┌─────────────────────────────────┐
│ getVerificationReport(path)     │
└─────────────────────────────────┘
    │
    ▼
┌─────────────────────────────────┐
│ Check Context Map               │
│ verificationReports.get(path)   │
└─────────────────────────────────┘
    │
    ├─── Found? ────┬─── Not Found?
    ▼               ▼
┌─────────┐    ┌─────────┐
│ Return  │    │ Return  │
│ Report  │    │undefined│
└─────────┘    └─────────┘
    │               │
    ▼               ▼
┌─────────┐    ┌─────────┐
│ Display │    │ Show    │
│ Results │    │ Empty   │
│ (instant)│   │ State   │
└─────────┘    └─────────┘
```

## 🔃 Lifecycle: Component Unmount/Remount

```
Initial Mount
    │
    ▼
┌─────────────────┐
│ Component       │
│ Renders         │
│ (shows data)    │
└─────────────────┘
    │
    ▼
┌─────────────────┐
│ User switches   │
│ to another tab  │
└─────────────────┘
    │
    ▼
┌─────────────────────────────────┐
│ Component UNMOUNTS              │
│ • Local state destroyed ❌      │
│ • Context state preserved ✅    │
│ • localStorage untouched ✅     │
└─────────────────────────────────┘
    │
    ▼
┌─────────────────┐
│ User switches   │
│ back to tab     │
└─────────────────┘
    │
    ▼
┌─────────────────────────────────┐
│ Component REMOUNTS              │
│ • Calls getVerificationReport() │
│ • Context returns cached data   │
│ • Data appears instantly ✅     │
└─────────────────────────────────┘
```

## 🌐 Page Refresh Flow

```
Page Load
    │
    ▼
┌─────────────────────────────────┐
│ WorkspaceProvider initializes   │
└─────────────────────────────────┘
    │
    ▼
┌─────────────────────────────────┐
│ useState initialization runs    │
│ with initializer function       │
└─────────────────────────────────┘
    │
    ▼
┌─────────────────────────────────┐
│ Check localStorage:             │
│ const saved = localStorage      │
│   .getItem('verificationReports')│
└─────────────────────────────────┘
    │
    ├─── Found? ────┬─── Not Found?
    ▼               ▼
┌──────────────┐ ┌──────────────┐
│ Parse JSON   │ │ Return empty │
│ Create Map   │ │ Map()        │
└──────────────┘ └──────────────┘
    │               │
    ▼               ▼
┌─────────────────────────────────┐
│ State initialized with data     │
└─────────────────────────────────┘
    │
    ▼
┌─────────────────────────────────┐
│ Components render with data ✅  │
└─────────────────────────────────┘
```

## 🗂️ Storage Structure

```
localStorage
│
├── verificationReports
│   │
│   ├── "/model-cards/example.md"
│   │   ├── consistency_score: 0.75
│   │   ├── claims_spec: {...}
│   │   ├── evidence_table:
│   │   │   ├── algorithms: [...]
│   │   │   ├── leakage: [...]
│   │   │   ├── metrics: [...]
│   │   │   └── ...
│   │   └── metrics_diffs: {...}
│   │
│   └── "/model-cards/another.md"
│       └── {...}
│
└── notebookDiscrepancies
    │
    ├── "/notebooks/notebook1.ipynb"
    │   ├── [0]
    │   │   ├── type: "leakage"
    │   │   ├── severity: "error"
    │   │   ├── message: "..."
    │   │   └── codeSnippet: "..."
    │   └── [1]
    │       └── {...}
    │
    └── "/notebooks/notebook2.ipynb"
        └── [...]
```

## 🎯 Key/Value Pairs

```
Context Map (In Memory)
┌────────────────────────────────────────┐
│ Key: String (file path)                │
│ Value: Object (verification data)      │
├────────────────────────────────────────┤
│ "/model-cards/ex.md" ──► Report{...}   │
│ "/notebooks/nb1.ipynb" ──► Issues[...] │
└────────────────────────────────────────┘

localStorage (Persisted)
┌────────────────────────────────────────┐
│ Key: String (storage key)              │
│ Value: String (JSON serialized)        │
├────────────────────────────────────────┤
│ "verificationReports" ──► "{...}"      │
│ "notebookDiscrepancies" ──► "[...]"    │
└────────────────────────────────────────┘
```

## 🔐 Error Handling Flow

```
Operation (Save/Load)
    │
    ▼
┌──────────────────┐
│ try {            │
│   operation()    │
│ }                │
└──────────────────┘
    │
    ├─── Success? ──┬─── Error?
    ▼               ▼
┌─────────┐   ┌──────────────────┐
│ Return  │   │ catch (error) {  │
│ Result  │   │   console.error() │
│         │   │   fallback()     │
└─────────┘   └──────────────────┘
                   │
                   ▼
              ┌──────────────────┐
              │ Graceful         │
              │ Degradation:     │
              │ • Use memory     │
              │ • Show error msg │
              │ • Continue app   │
              └──────────────────┘
```

## 📊 State Comparison

### ❌ Before (Local State)
```
Component A              Component B
    │                       │
    ▼                       ▼
┌─────────┐           ┌─────────┐
│ state A │           │ state B │
│ (local) │           │ (local) │
└─────────┘           └─────────┘
    │                       │
    ▼                       ▼
  Unmount                 Unmount
    │                       │
    ▼                       ▼
  ❌ Lost                 ❌ Lost
```

### ✅ After (Context + localStorage)
```
Component A              Component B
    │                       │
    ▼                       ▼
    ╔═════════════════════════╗
    ║   Shared Context        ║
    ║   ├─ Map (memory)       ║
    ║   └─ localStorage       ║
    ╚═════════════════════════╝
    │                       │
    ▼                       ▼
  Unmount                 Unmount
    │                       │
    ▼                       ▼
  ✅ Persists             ✅ Persists
```

## 🎬 Sequence Diagram

```
User          Component       Context        localStorage
 │                │              │                │
 │ Click Verify   │              │                │
 ├───────────────►│              │                │
 │                │              │                │
 │                │ API Call     │                │
 │                │──────────┐   │                │
 │                │          │   │                │
 │                │◄─────────┘   │                │
 │                │ Result       │                │
 │                │              │                │
 │                │ saveReport() │                │
 │                ├─────────────►│                │
 │                │              │                │
 │                │              │ setItem()      │
 │                │              ├───────────────►│
 │                │              │                │
 │                │              │                ✓
 │                │              │                │
 │ [Switch Tab]   │              │                │
 │                │ UNMOUNT      │                │
 │                │              │                │
 │ [Switch Back]  │              │                │
 │                │ MOUNT        │                │
 │                │              │                │
 │                │ getReport()  │                │
 │                ├─────────────►│                │
 │                │              │                │
 │                │◄─────────────┤                │
 │                │ Cached Data  │                │
 │                │              │                │
 │◄───────────────┤              │                │
 │ Display ✅     │              │                │
```

## 🚀 Summary

The architecture ensures:
- **Global State**: Via React Context
- **Fast Access**: Via in-memory Maps
- **Persistence**: Via localStorage
- **Reliability**: Via error handling
- **Performance**: Via caching

**Result**: No data loss! 🎉

