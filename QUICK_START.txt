╔════════════════════════════════════════════════════════════════════════════╗
║           AST-RAG MODEL CARD CHECKS - QUICK START GUIDE                   ║
╚════════════════════════════════════════════════════════════════════════════╝

📦 UNIFIED CONFIGURATION (Single .env file for both services!)

┌────────────────────────────────────────────────────────────────────────────┐
│ STEP 1: Setup Configuration                                               │
└────────────────────────────────────────────────────────────────────────────┘

Run the interactive setup:

    ./setup-env.sh

This creates a single .env file at project root that BOTH services use.

┌────────────────────────────────────────────────────────────────────────────┐
│ STEP 2: Start Services                                                    │
└────────────────────────────────────────────────────────────────────────────┘

Start everything with one command:

    ./start-all-services.sh

OR start individually:

    # Terminal 1 - Backend
    cd services/codeact_cardcheck && ./start_api_server.sh

    # Terminal 2 - Frontend  
    cd apps/api && pnpm dev

┌────────────────────────────────────────────────────────────────────────────┐
│ STEP 3: Open Application                                                  │
└────────────────────────────────────────────────────────────────────────────┘

Navigate to: http://localhost:3001/workspace

┌────────────────────────────────────────────────────────────────────────────┐
│ KEY FEATURES                                                               │
└────────────────────────────────────────────────────────────────────────────┘

✅ Single .env configuration for both frontend and backend
✅ API keys work from BOTH .env file AND UI settings
✅ Automatic environment loading on startup
✅ Health checks verify service connectivity
✅ Comprehensive error messages with diagnostics

┌────────────────────────────────────────────────────────────────────────────┐
│ API KEY PRIORITY                                                           │
└────────────────────────────────────────────────────────────────────────────┘

1. UI Settings (LLM Settings dialog) - highest priority
2. Root .env file - automatic fallback
3. System environment variables - last resort

This means you can:
- Set keys in .env for persistent configuration
- Override via UI without editing files
- Use environment variables in CI/CD

┌────────────────────────────────────────────────────────────────────────────┐
│ CONFIGURATION FILES                                                        │
└────────────────────────────────────────────────────────────────────────────┘

📍 Project Root:
   .env                    ← Single source of truth (gitignored)
   env.config.template     ← Template with all options
   setup-env.sh           ← Interactive setup script
   start-all-services.sh  ← Start both services

📍 Documentation:
   UNIFIED_ENV_CONFIG.md  ← Detailed configuration guide
   README.md              ← Main documentation

┌────────────────────────────────────────────────────────────────────────────┐
│ TROUBLESHOOTING                                                            │
└────────────────────────────────────────────────────────────────────────────┘

❌ Backend won't start?
   → Check: cat .env | grep API_KEY
   → Verify keys are set (not placeholder values)

❌ Frontend can't connect?
   → Check: curl http://localhost:8001/health
   → Ensure backend started successfully

❌ API keys not working?
   → Verify format: sk-ant-... (Anthropic) or sk-... (OpenAI)
   → Test key at provider's website

❌ Still having issues?
   → Check logs in both terminal windows
   → See UNIFIED_ENV_CONFIG.md for detailed troubleshooting

┌────────────────────────────────────────────────────────────────────────────┐
│ HEALTH CHECKS                                                              │
└────────────────────────────────────────────────────────────────────────────┘

Backend health:    curl http://localhost:8001/health
Frontend health:   curl http://localhost:3001/api/health

Both should return JSON with status information.

╔════════════════════════════════════════════════════════════════════════════╗
║  For more details: see UNIFIED_ENV_CONFIG.md                              ║
╚════════════════════════════════════════════════════════════════════════════╝

