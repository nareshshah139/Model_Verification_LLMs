# Leakage exclusions rulepack

# Alert if post-origination fields appear in features
- id: possible-leakage-columns
  language: python
  rule:
    regex: '\b(out_prncp|total_pymnt|recoveries|last_pymnt_d|last_pymnt_amnt|out_prncp_inv)\b'

